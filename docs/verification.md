# 实际验证记录

最新验证日期：2026-09-21。环境：Windows 11，Anaconda命名环境 `travel-planner`，Python 3.12.14。

## 自动化验证

2026-09-21：`python -m pytest -q -W error::RuntimeWarning` 全量134项通过；`python -m ruff check src frontend scripts tests` 通过。复杂语义新增回归、真实接口记录和能力边界见 [复杂需求理解](semantic-understanding.md)。本轮未重新测量覆盖率；下面覆盖率为此前89项测试的历史记录。

执行：

```text
python -m pytest -q -W error::RuntimeWarning --cov=src --cov-report=json:data/coverage.json
89 passed, 1 warning in 25.64s
```

语句覆盖率 **85.37%**（2124 / 2488）。这不是分支覆盖率，也不证明所有可能的出行请求均正确。

唯一警告来自安装版本的Starlette对AnyIO旧别名的弃用提示。项目超时路径的未等待协程警告已修复，并使用 `-W error::RuntimeWarning` 防止回归。

`python -m ruff check src frontend scripts tests`：通过。

`python -m pip check`：No broken requirements found。

| 必需E2E场景 | 验证内容 | 结果 |
|---|---|---|
| 夜间停运 | 能赶上的公共交通后接短途交通，并有允许的直达打车比较 | 通过 |
| 预算极限 | 京津50元内、不打车；0元无候选不编造班次 | 通过 |
| 多换乘 | 甲县 → 甲枢纽 → 乙枢纽 → 乙县 | 通过 |
| 政策问答 | 铁路充电宝答案有12306来源，不套用于飞机 | 通过 |
| 偏好记忆 | 长期禁用打车可复用，本次允许不覆盖长期记录 | 通过 |
| 异常降级 | 工具失败无缓存时明确缺数据；有效缓存回退 | 通过 |
| 熔断 | 6轮上限、短deadline、Token调用前拒绝超额请求 | 通过 |
| 风险标注 | 夜间单车、演示天气、非官方与末班风险 | 通过 |

其他回归包含：早到但较贵标签不能被便宜晚到标签误删、跨午夜、已错过末班、累计骑行距离、改写费用被最终校验拒绝、缓存重试期间过期不可使用、供应商返回错误Schema后重试、LLM非法JSON修复失败后规则降级、HTTP模型usage记账与调用缓存、会话删除和隔离、可选Bearer保护。

## 运行与浏览器验证

2026-09-20 指定时间解析修复：

- `python -m pytest tests/test_requested_time.py -q`：15 passed。
- 中文冒号、全角数字及冒号周围空格统一识别；精确出发时刻不会降为“晚上”时间窗。只改时刻的追问保留原出行日期。
- 测试从用户原句一直检查至高德实际请求参数 `time=22-30`，同时覆盖有可行路线和末班已过两种响应。
- 2026-09-17真实接口重放原句，返回出发时间 `2026-09-17T22:30:00+08:00`；本轮公共交通候选未通过末班检查，没有改成当前时间。记录见 `data/requested-time-verification.json`。

2026-09-16 自动选站验证：

- 用户原句“10点半从鸟巢出发，去前往北京印刷学院，四号线赶不上了，先坐地铁，然后打车，给我花费最低的方案”已进入真实地图选站流程。服务联调记录在 `data/night-planning-result.json`，站名由地图返回、程序选择。
- 联调修复高德 geocode 对“鸟巢”的同名商户歧义，以及路线接口站点 ID 与线路列表站点 ID 不同的问题。线路扩展不再依赖两接口 ID 一致。
- 新增11项回归，覆盖自然语言单程路由、旧多站会话替换、预算追问保留接驳要求、站点 ID 差异、最近站已停运、按地铁加打车总费用选站、缺失/紧张末班、跨午夜末班、不可等待早班冒充夜间接驳、未知打车费用、禁用方式及过去时间。
- 地铁候车与打车等待是显式估算；本轮推荐不构成全网最低价证明。
- 已重启本地服务并在 Gradio 页面提交原句：聊天直接推荐下车站，卡片显示“推荐：五福堂下车再打车”，该次查询地铁参考6元加出租车估价34元。此为2026-09-16查询记录，不是写死的站点或未来报价；夜间加价等未核实。

2026-09-15 补充验证：

- 主动规划：真实地理编码、公交/铁路路线证据、步行接驳已联调；“清源路→天津奥体→自动选择海底捞→广州”的明确日期案例产生覆盖三站的候选。其铁路服务日期、库存、实际票价、夜间住宿仍待核实，不作为已验票的出行保证。
- 约束冲突：设定更早的最终到达期限时，保留已完成活动，后续不合格路线单独列为“需要放宽条件”的建议。
- 修复中文日期/“19点半”/“吃一小时”解析、活动开场误作最终期限、餐厅错误固定在散场时刻，以及同一供应商对象中打车接驳必须按站点关系排在列车之前。
- 当前 78 项测试包含上述回归、整趟成本选择门店、保留部分结果、显式取消期限和未知打车费用标注。

- DeepSeek 真实 JSON 探针通过，高德真实 POI 探针通过（返回 5 条场馆候选）；凭据仅写入本地 `.env`。
- 浏览器重放“清源路→天津奥体演唱会→海底捞→广州”，保留活动顺序、按缺失时间追问，展示高德检索到的门店。没有误回复“已保存偏好”。
- 联调发现模型可能遗漏明确给出的最晚到达时间；已增加规则确定值覆盖与历史缺失值补回，并加入回归测试。
- 新测试覆盖累计餐饮预算、过夜限制、错过活动开始、较贵早到标签保留以赶后续班次、同地点活动、结构化多站完整求解，以及 POI 响应校验与缓存。交通规划测试均使用合成网络。

以下环境初始化与夜间演示结果为 2026-09-14 的验证记录：

- `conda run -n travel-planner python ...` 确认解释器为 `D:\anaconda3\envs\travel-planner\python.exe`。
- 数据库初始化、示例数据导出、Chroma索引构建均成功，政策索引含5个条目块。
- `python -m scripts.demo`：京津3套、夜间3套、县域1套、政策回答带引用；结果保存在 `data/demo-results.json`。
- Uvicorn在 `127.0.0.1:8000` 成功启动，Gradio页面加载、提交与独立方案卡片已在浏览器实测。
- 夜间合成示例：地铁+短途网约车¥22/43分钟，地铁+步行¥4/70分钟，全程网约车¥80/32分钟；均显示合成数据与估价风险。这些数字是演示数据，不是真实报价。
- 方案对比区、可展开风险和结构化结果可见；聊天提交后输入清空，保留同会话上下文。
- 浏览器内保存骑行与禁用打车偏好、提交政策问题均成功；验证后恢复默认演示偏好。

## 本地性能测量

执行 `python -m scripts.benchmark`，连续25个合成京津预算请求，包含规划和SQLite记录保存，不包含启动/建库时间，也没有真实网络或LLM调用。

| 指标 | 实测 |
|---|---:|
| 平均耗时 | 41.42 ms |
| P50 | 36.23 ms |
| P95（25个样本的第24个顺序统计量） | 85.59 ms |
| 最大耗时 | 87.89 ms |

原始测量在 `data/benchmark.json`。该结果不能外推为全国交通图性能、并发性能或真实API端到端时延。8000 Token限制测试只证明本地调用预算逻辑，未测量付费模型账单。

## 尚未验证与后续验收

- 本机未安装可调用的Docker，Dockerfile和compose已提供，但未实测镜像构建和容器启动。
- DeepSeek、高德 POI 与地图路线已完成真实联调；同程机票、售票方库存和天气尚未接入。地图中的列车号和参考价格不等于当日可预订票务，也不证明全局最低价。
- CI配置已提供，尚未在GitHub Actions运行；Python3.10/Linux兼容性尚未实机验证。
- RAG使用离线字符向量与确定性引用生成，不是预训练语义检索质量评测；政策持续更新需维护人员核验。
- 小众路线真实可用性、全量站内步行和换乘时间、单车库存、出租车实际费用与交通拥堵尚未接入。
- 没有证明全局最优或无解；所有结论限于所提供数据、Top-K与搜索资源上限。
